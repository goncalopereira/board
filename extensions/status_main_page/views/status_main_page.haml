%html
	%head
		%title Current Status
	%body		
		%h2 Current Status

		%table
			%tbody
			- locals[:services].each do |service|
				%tr
					%td= service['name']
					%td= service['current-event']['status']	
		
		%table
			%tbody				
			-locals[:statuses].each do |status|
				%tr
					%td= status['name']
					%td= status['description']
					%td
						%img{:src=> "#{locals[:url]}#{status['image']}"}
